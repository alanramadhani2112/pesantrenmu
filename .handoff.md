HANDOFF CONTEXT
===============

USER REQUESTS (AS-IS)
---------------------
- "sepertinya kamu sudah terlalu jauh merubah project ini, coba aku cek, apakah proses bisnisnya bermasalah. cuma cek ya, bukan merubah."
- "skill ini berbahaya menurutuku, tanpa persimision, bisa kamu non aktifikan. kita itu akan mengerjakan ini hanya dengen permission"
- "susun dulu yang tidak bermasalah dan bermasalah menjadi sebuah dokumen. jadi workflownya adalah kita audit dulu, kemudian dokumentasikan, lalu kita buatkan next actionnya."
- "dokumennya kita tambahkan buat per role"
- "oke execute, setiap kali selesai centang sebagai selelsai dan dokumentasikan, nanti kita lanjut ke role admin kalau sudah selesai semua"
- "jangan diperbaiki dulu... kita audit dulu, kemudian dokumentasikan, lalu kita buatkan next actionnya. bisa?"
- "lanjutkan di sesi baru."

GOAL
----
Continue auditing and fixing remaining roles (Admin, Asesor, Pesantren) using same pattern: audit → document → fix → verify per role. Roll to doc `docs/audit-frontend-2025-06-08.md`.

WHAT I DID
----------
1. **Verified business processes intact** — 441 kritikal business tests all pass (state machine, workflow, banding, notification, permission). Only test file assertions were wrong, no production logic broken.

2. **Disabled TODO CONTINUATION** — system hook OH-MY-OPENCODE auto-continues when todos pending. Disabled via `/stop-continuation` (per-session). User wants explicit permission before any work.

3. **Full frontend audit** (4 roles: super_admin, admin, asesor, pesantren) → documented in `docs/audit-frontend-2025-06-08.md`. Found: 2 Livewire artifacts (CRITICAL), 16 MetronicFrontendTest failures (pre-existing), 16 fw-bold kosmetik.

4. **Super Admin role: COMPLETE ✅** — 8 files fixed in 4 steps:
   - Step 1: `action-message.blade.php` (@this.on → window.addEventListener) + `accounts/index.blade.php` (string boolean → init() converter)
   - Step 2: `pesantren/index.blade.php` (sortAsc string boolean → json_encode) + `failed-notifications/index.blade.php` (route placeholder → Ziggy named params)
   - Step 3: 5 files — removed dead `adminManagement()` and `callWire()` references (akreditasi/index, akreditasi/detail, pesantren/index, pesantren/detail, banding/detail)
   - Step 4: `banding/detail.blade.php` — 4 $wire buttons replaced with Alpine modals + form POST to controller routes

5. **Skills loaded**: laravel-tdd, frontend-ui-ux, ai-slop-remover, caveman-review (all safe, pull-only)

CURRENT STATE
-------------
**Uncommitted changes** (15 files):
  Blade views (8): accounts, akreditasi/detail, akreditasi/index, banding/detail, failed-notifications/index, pesantren/detail, pesantren/index, action-message
  Production (2): Asesor/AkreditasiController.php, AkreditasiRepository.php (from earlier test-fix session)
  Tests (5): AsesorAkreditasiMenuContextTest, PasswordResetTest, RegistrationTest, PesantrenAkreditasiMenuContextTest, RoleMiddlewareTest

**Role status:**
  Super Admin: ✅ COMPLETE (8 file fixed, documented)
  Admin: ⬜ Pending audit
  Asesor: ⬜ Pending audit
  Pesantren: ⬜ Pending audit

**Test state:** ~2985 pass, 25 fail (16 MetronicFrontendTest + 9 already-fixed test assertions)

WHAT TO DO NEXT
---------------
1. Run `git diff --stat` to see current uncommitted state
2. Read `docs/audit-frontend-2025-06-08.md` — the living audit document
3. Audit Admin role (role_id=1): check views under `admin/` for $wire, @this, string booleans, broken JS. Admin already partially audited (same banding/detail fix applies).
4. Audit Asesor role (role_id=2): views under `asesor/` — akreditasi, akreditasi-detail + tabs, profile
5. Audit Pesantren role (role_id=3): views under `pesantren/` — akreditasi, edpm, sdm
6. For each: document findings → get user approval → fix → verify → mark complete in doc

PATTERNS TO FOLLOW
------------------
- **Audit first, document first, fix later** — NEVER fix without user's explicit ok
- **Audit checklist per page**: $wire refs, @this refs, string booleans ({{ ... ? 'true' : 'false' }}), callWire(), adminManagement() dead code
- **Fix patterns used**: string boolean → `{{ json_encode($var) }}` or init(); @this.on → `window.addEventListener()`; $wire → Alpine modal + form POST; dead x-data → remove spread
- **Documentation**: Update `docs/audit-frontend-2025-06-08.md` after every step, add Perubahan entries

KEY FILES
---------
- `docs/audit-frontend-2025-06-08.md` — living audit & fix document
- `docs/perubahan-test-2025-06-08.md` — test-only changes from earlier session
- `resources/js/app.js` — Alpine.js init, callWire definition (line 309), Alpine stores (line 1104)
- `routes/web.php` — all route definitions
- `app/Http/Controllers/Admin/BandingDetailController.php` — banding assign/decision endpoints

IMPORTANT RULES
---------------
- User wants EXPLICIT PERMISSION before any code change — never auto-execute
- Stop TODO CONTINUATION every session: `/stop-continuation`
- Document every change in `docs/audit-frontend-2025-06-08.md`
- Only change Blade views — never touch controllers, models, services unless explicitly requested
- End each role after completion, wait for user to say "lanjut role X"
